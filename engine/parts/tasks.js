const algorithms = require('../algorithms')

class Tasks {
  constructor ({situation, target}) {
    if (!situation) {
      // Standart algorithm
    }

    switch (situation) {
      case ''
      default:
        return algorithms.Queue()
    }
  }
}

module.exports = Tasks